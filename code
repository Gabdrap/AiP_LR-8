#include <iostream>
#include <Windows.h>
#include <iomanip>

using namespace std;
char a;
char b;
char sum;
char (symbol);

int main()
{
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    int choise;
    cout << "1" "\n";
    cout << "2" "\n";
    cout << "3" "\n";
    cout << "Выберите задание" "\n";
    cin >> choise;

    if (choise == 1)
    {
        char st[6];
        char ch;
        cout << "type characters and press Enter: ";
        cin >> st;
        ch = st[0];
        st[0] = st[4];
        st[4] = ch;
        cout << "the result is: " << st;
        cin.get();
        cin.get();
        return 0;
    }
    if (choise == 2)
    {
        cout << "Ввeдитe символы: ";
        cin >> a >> b;
        int sum = a + b;
        cout << sum;
    }
    if (choise == 3)
    {
        cout << "Введите символ: ";
        cin >> noskipws >> symbol;
        (int)symbol;
        if ((symbol > 31, symbol < 48) || (symbol > 91, symbol < 96))
        {
            cout << "Этот символ управляющий";
        }
    }
}
